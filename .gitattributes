# Normalise les fins de ligne : le dépôt est consommé tel quel par d'autres
# projets, un CRLF qui traîne produirait des diffs fantômes selon la machine.
* text=auto eol=lf

# Fichiers binaires : jamais de conversion.
*.png binary
*.jpg binary
*.woff binary
*.woff2 binary
